using UnityEngine;

/// <summary>
/// Initial/Static platform that doesn't move.
/// Used at game start so player doesn't stand in air.
/// </summary>
public class StaticPlatform : MonoBehaviour
{
    // This platform is static and doesn't move
    // No Update needed
}
